{
  "scripts": {
    "build": "mkdir dist && tailwindcss -i ./src/index.css -o ./dist/style.css --minify && cp -r ./src/*.html ./dist/"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.4"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
